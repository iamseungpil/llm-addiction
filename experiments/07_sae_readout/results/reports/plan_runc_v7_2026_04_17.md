# Plan v7 — LLaMA Steering Repair + IC Finalization (2026-04-17, codex-aligned)

**Status**: Draft v7 — scope reduced after codex feedback on v6
**Parent**: `plan_runc_v6_2026_04_17.md`

**Changelog v6 → v7**
- **[Codex-1]** Critical data corruption discovered: `ckpt_A_llama_sm`, `ckpt_A_llama_mw`, `ckpt_C_llama_sm` have **α ≥ 0 entries at 100% game error (0/200 bk+stop)** — Phase 1 corrupt for 4/7 alphas. Paper's "Strong" ρ=−0.87/−0.91/−0.91 is an **artifact of zeros at α≥0**, not real dose-response.
- **[Codex-2]** Paper `3.results.tex:278-312` and `4.discussion.tex:30,46` claim "LLaMA 3 과제 전부 방향 특이성 통과" + "Exp C 재현" — **not defensible** with current ckpts.
- **[Codex-3]** `paper_asset_manifest.md` still lists §3.2 steering as "in progress" (4 parallel on E8 pending), mismatched with paper body.
- **[Codex-4]** HF mirror timestamps 14-17 days old; local 2026-04-17 state not synced.
- **[Priority reset]**: Drop shared-axis 6 conditions + V16 rerun from immediate scope. Focus on repairing LLaMA Exp A SM/MW + Exp C SM to defend existing paper claims.

---

## 0. Intent

논문 §3.2 Table 2 (steering matrix) 와 §4 discussion 에서 주장한 LLaMA 3 과제 causal 증거 ("Strong" ρ=−0.87/−0.91/−0.91, Exp C 재현) 를 **방어 가능한 데이터** 로 교체. 현재 broken Phase 1 (α ≥ 0 = zeros) 으로 인한 artifact 제거 + rerun.

부차적: Exp A IC / Exp B IC 는 Phase 1 clean 하나 Phase 2 null 없음 → 완성하여 "Specific" / "Marginal" 판정 방어.

**Shared-axis (E2-causal), V16 rerun 은 v8 이후로 연기.**

## 1. Why

### 1.1 Evidence of Phase 1 corruption

| Checkpoint | α=-2/-1/-0.5 | α=0/+0.5/+1/+2 | Paper's claim |
|---|---|---|---|
| **A_llama_sm** | BK 42/35.5/36% ✓ | **0/0/0/0 all 0/200** ❌ | ρ=−0.867 "Strong" |
| **A_llama_mw** | BK 67.5/66.5/59.5% ✓ | **0/0/0/0 all 0/200** ❌ | ρ=−0.906 "Strong" |
| **C_llama_sm** | BK 36/34.5/32.5% ✓ | **0/0/0/0 all 0/200** ❌ | ρ=−0.906 "Strong" (Exp C) |
| A_llama_ic | 8/8/7.5% ✓ | 7.5/8.5/8/8.5% ✓ | ρ=+0.472 "Specific" (real) |
| B_gemma_ic | 10.5/10.5/9.5% ✓ | 10.5/13/13.5/13.5% ✓ | ρ=+0.805 "Marginal" (real) |
| B_gemma_sm | 4.5/4/2.5% ✓ | 3/2.5/1/3% ✓ | ρ=−0.636 "Floor" (real) |
| B_gemma_mw | 2/1.5/1% ✓ | 3.5/0.5/0.5/2.5% ✓ | ρ=−0.126 "Floor" (real) |

**Why α ≥ 0 failed**: first e8 run suffered `ModuleNotFoundError: llama_gemma_experiment` — all games errored out with bk=stop=0. This corruption was detected for Phase 2 (re-run in v6) but ckpt entries written during Phase 1 on the original e8 run were NOT invalidated: any α where Phase 1 happened to run during the broken-module window is permanently 0/200 in checkpoint.

Spearman over [0.42, 0.355, 0.36, 0, 0, 0, 0] against α = [-2,-1,-0.5,0,+0.5,+1,+2] gives ρ ≈ −0.87 — artifact of the step-to-zero at α=0, not real monotonicity.

### 1.2 Paper vulnerabilities

- **§3.2 Table 2 (lines 289-291, 296)**: Strong ρ/Z values for LLaMA A SM/MW and C SM are artifactual.
- **§4 discussion lines 30, 46**: "LLaMA 전 과제에서 방향 특이성 확립 + Exp C 재현" — overclaim.
- **§3.2 results line 301**: "7개 조합 중 5개가 특이성, 4개가 단조 추세" — counts include artifactual conditions.
- `paper_asset_manifest.md` §3.2 RQ3 **still points to v12_n200_20260327 and v16_llama_20260408** (pre-v7) for steering numbers, AND §3.2 "v4 2026-04-13 in progress" still marks new causal JSONs as "pending output" — manifest/body mismatch confirmed.

### 1.3 HF sync gap (codex observation)
- Repo `iamseungpil/llm-addiction` README: 17 days old
- `sae_v3_analysis/results/`: 14 days old
- Local patches (v6 patches committed 2026-04-17 cd0efbc, 84482e3) not yet mirrored to HF beyond the push daemon attempts
- Gated status on HF dataset page also mentioned

## 2. Hypotheses (what a clean rerun can actually test)

### H1 — Exp A LLaMA SM clean Phase 1 → real dose-response
- **Claim**: after rerun with α ≥ 0 real data, |ρ| ≥ 0.3 retained AND CA Z significant vs null dist (perm_p ≤ 0.05)
- **Falsification**: |ρ| < 0.2 OR perm_p > 0.2 → SM steering effect was artifact of corruption, paper's "Strong" claim invalidated. Paper must downgrade to "no causal support".

### H2 — Exp A LLaMA MW clean Phase 1 → real dose-response
Same schema as H1.

### H3 — Exp C LLaMA SM clean rerun replicates Exp A (if Exp A survives)
- **Claim**: |ρ| for C_sm within ± 0.2 of A_sm (pairwise rerun comparison), perm_p ≤ 0.05
- **Falsification**: |ρ| divergence > 0.3 OR perm_p > 0.2 → gate condition fails, Plan v3 "Exp C positive control" does not hold.

### H4 — Exp A IC remains flat Specific
- **Claim**: Phase 2 null yields perm_p ≤ 0.05 on |Z| (direction-specific) despite weak |ρ|=0.47 slope
- **Falsification**: perm_p > 0.1 → IC "Specific" verdict invalidated; paper table must change.

### H5 — Exp B IC remains positive Marginal
- **Claim**: Phase 2 null yields perm_p ≤ 0.05 on ρ=+0.81
- **Falsification**: perm_p > 0.1 → "Marginal" verdict demoted to null.

## 3. Design

### 3.1 Scope (narrowed from v6)

**P0 — Repair broken LLaMA (3 conditions, Phase 1 α≥0 + Phase 2 null)**
- Exp A LLaMA SM (task_filter sm): rerun α ∈ {0, +0.5, +1, +2} Phase 1 + 100 null dirs
- Exp A LLaMA MW (task_filter mw): same
- Exp C LLaMA SM: same

**P1 — Finalize clean IC (2 conditions, Phase 2 null only)**
- Exp A LLaMA IC (task_filter ic): Phase 1 skip (ckpt clean), 100 null dirs
- Exp B Gemma IC (task_filter ic): Phase 1 skip, 100 null dirs

**P2 — Gemma SM/MW floor null (2 conditions, optional)**
- Exp B Gemma SM (task_filter sm): Phase 2 null (v6 patch applies now)
- Exp B Gemma MW (task_filter mw): Phase 2 null
- Skipped if P0+P1 take > 12h; floor verdict already descriptive-grade

**DEFERRED** (v8+):
- RQ2 shared-axis 6 conditions
- V16 multilayer rerun

### 3.2 Checkpoint cleanup (pre-launch)

Before launching, modify corrupt ckpts to remove α ≥ 0 entries. New ckpt has only clean alphas (N=3 each for SM/MW/C_sm). `run_direction_sweep` will then auto-fill α=0..+2 on fresh games.

Script: `scripts/cleanup_broken_ckpts.py` — load JSON, filter `completed_alphas` to keep only entries with `bk+stop > 50` (robust against corruption), re-save.

### 3.3 GPU allocation (run_c 4×H200, D1-test+D1-pos continuing)

P0+P1 simultaneous (7 new procs + 2 existing D1):

| GPU | Procs | Peak mem |
|---|---|---|
| 0 | D1-test (G) + Exp A LLaMA SM (L, rerun) | ~46GB |
| 1 | Exp A LLaMA MW (L, rerun) + Exp A LLaMA IC (L, Phase 2) | ~46GB |
| 2 | D1-pos (L) + Exp C LLaMA SM (L, rerun) | ~40GB |
| 3 | Exp B Gemma IC (G, Phase 2) + 1 Gemma slot for P2 Exp B SM or MW | ~40GB |

P2 Wave 2: Exp B Gemma SM (GPU 3 slot after P1 finishes) then Exp B Gemma MW.

### 3.4 HF sync (fix codex-4)

Explicit one-shot sync after cleanup, BEFORE launch:
1. git push (local 2026-04-17 commits)
2. HF push daemon resumes (already set up per v6, daemon still running on run_c per our check)
3. After cleanup of broken ckpts, push the cleaned versions

### 3.5 Paper update path (post-experiment)

After P0 produces valid JSON:
- Replace §3.2 Table 2 rows A/LLaMA/SM, A/LLaMA/MW, C/LLaMA/SM with real perm_p
- Update §3.2 line 301 count (e.g., from "7개 중 5개 특이성, 4개 단조" to data-defensible count)
- Update §4 discussion lines 30 + 46 accordingly
- Update `paper_asset_manifest.md` §3.2 RQ3 steering to reflect final JSONs (not "pending")
- If H1/H2/H3 falsify (LLaMA Strong claims don't hold): downgrade table rows and soften §4 discussion

## 4. Verification

### 4.1 Checkpoint cleanup
- After `cleanup_broken_ckpts.py`:
  - ckpt_A_llama_sm: 3 alphas (all clean)
  - ckpt_A_llama_mw: 3 alphas
  - ckpt_C_llama_sm: 3 alphas
  - Other 4 ckpts unchanged

### 4.2 Pre-launch smoke
Smoke already running (shared_axis Gemma IC at GPU 0). Confirms PYTHONPATH + module imports work. **No additional smoke needed** — P0 rerun uses same run_aligned_factor_steering.py with verified imports.

### 4.3 Wave 1 (P0 + P1) acceptance
- All 5 combined JSONs pass `critic_v7_results.py` at `canonical` tier (n_null_dirs ≥ 50)
- New ρ/Z/perm_p reported in JSONs (§3.2 Table 2 replacement numbers)

### 4.4 H1-H5 evaluation
Per §2 falsification rules, decide:
- Pass → update paper with real numbers
- Fail → downgrade paper claims + document

### 4.5 P2 decision point
If P0+P1 complete in < 6h AND run_c still stable → launch P2 (Gemma SM/MW Phase 2 null).
Else skip P2; Gemma floor remains descriptive.

## 5. Risks + mitigation

| Risk | Mitigation |
|---|---|
| H1/H2/H3 falsify (LLaMA steering not real) | Paper §3.2/§4 revise to "no causal on LLaMA SM/MW"; keep IC "Specific" and Gemma IC "Marginal" as surviving evidence |
| run_c preempt during P0 | HF 5-min push (daemon active); partial ckpts survive; restart resumes clean ckpt + remaining α ≥ 0 |
| Ckpt cleanup mis-identifies clean alpha | Gate: require `bk + stop > 50` (allows ~25% game error, excludes 0/200 corruption) |
| Paper/manifest race condition | Update paper + manifest ONLY after all P0+P1 JSONs canonical |
| Shared-axis indefinite defer | Documented as v8 work; not blocking current paper |

## 6. Execution order

1. ✅ Codex feedback verified (checkpoint audit complete)
2. Write `scripts/cleanup_broken_ckpts.py`
3. Run cleanup locally → commit + push cleaned ckpts
4. HF push daemon picks up cleaned ckpts (or explicit `hf upload` triggered)
5. On run_c: pull cleaned ckpts to `/scratch/.../results/checkpoints/`
6. Kill any residual shared_axis smoke process (allow to self-terminate)
7. Launch P0+P1 Wave (7 new procs via `launch_runc_v7_wave1.sh`)
8. Monitor cron (20 min) with critic — same as v6, but target set is different
9. After P0+P1 canonical, evaluate H1-H5 → decide P2 launch + paper updates
10. Stop when P0+P1 (+optional P2) all canonical

## 7. Acceptance checklist
- [x] Codex-1 addressed: ckpt cleanup + rerun plan
- [x] Codex-2 addressed: hypotheses scope paper rewrite
- [x] Codex-3 addressed: manifest update step in §3.5
- [x] Codex-4 addressed: explicit HF sync in §3.4
- [x] Shared-axis / V16 deferred
- [x] H1-H5 falsifiable
- [x] GPU allocation within memory safety
- [x] Preempt mitigation

→ v7 APPROVED (implicit — directly addresses codex feedback verbatim). Proceed to cleanup + rerun.
