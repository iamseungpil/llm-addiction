# Track A1 / M2 Claim Surgery Branches (rebuttal §3.2 prose)

Per the codex Round 9 protocol mirrored in §9bis of `PLAN_4NODE_EXECUTION_2026_05_07.md`, the M2 outcome branches are committed in advance so prose does not chase results. Plan v4 §2.4 enumerates three readout templates; the prose below renders each into a §3.2 paragraph variant in the same template style as Track 0's `claim_surgery_§3.2_branches.md`. Day 5 selects the branch by the Plan §8.1 decision rule (lower 95% Wald CI on the primary contrast `Δ_{+G,first_person} − Δ_{+G,role_play_gambler}`) and inserts it verbatim into the rebuttal.

## M2-passes branch (primary CI excludes 0, positive direction)

> "The +G prompt manipulation is propensity-like rather than theatrical role uptake. Replicating the §3.1 protocol on six models under two framings (first-person decision-maker vs role-play-as-a-gambler), we find that the +G-induced shift in bankruptcy from BASE survives in first-person framing: the framing-gap contrast `Δ_{+G,first_person} − Δ_{+G,role_play_gambler}` is `[delta_gap_prob] [CI]` on the probability scale (`[delta_gap_logit] [logit CI]` on the logit scale), with the lower 95% Wald CI excluding zero. Role-play framing amplifies the +G effect, as expected (the framing manipulation check confirms a larger gambling-language boost under role-play, [N pass / 6] models), but does not constitute it: the same direction of effect appears in first-person. We therefore retain the §3.2 framing of +G as a *prompt-conditioned gambling-risk propensity*, with role-play uptake operating as an amplifier rather than the underlying mechanism."

## M2-mixed branch (some indicators or tasks pass, others do not)

> "The +G prompt manipulation is propensity-like for the primary risk indicator (bankruptcy under SM) but with task- and indicator-level heterogeneity. The framing-gap contrast `Δ_{+G,first_person} − Δ_{+G,role_play_gambler}` is `[delta_gap_prob] [CI]` for the SM bankruptcy outcome (lower CI > 0), but the secondary contrasts on round-level indicators (I_BA, I_LC, I_EC) and on the IC/MW robustness tasks show mixed separation: [list of pass indicators/tasks] retain a positive first-person boost while [list of fail indicators/tasks] do not separate from role-play. We therefore frame the §3.2 +G effect as propensity-like with an explicit caveat that role-play uptake is a partial driver for [list of fail surfaces], and report all secondary contrasts in the appendix. Section 6 takes up the consequence — that +G acts on a mixture of stable propensity and theatrical role uptake whose decomposition depends on which behavioral indicator is queried."

## M2-fails branch (primary CI includes 0; role-uptake hypothesis cannot be ruled out)

> "We narrow the §3.2 claim. Replicating the +G prompt manipulation under matched first-person and role-play-gambler framings, the framing-gap contrast `Δ_{+G,first_person} − Δ_{+G,role_play_gambler}` has lower 95% Wald CI overlapping zero (`[delta_gap_prob] [CI]`), and we cannot rule out the role-uptake hypothesis: the +G-induced shift on the primary risk metric does not survive a switch from role-play to first-person framing. We therefore refer to the §3 +G effect as *prompt-conditioned gambling-risk behavior* rather than *addiction-like propensity*, and we do not claim that +G isolates a stable trait independent of the role-play scaffold. Section 6 records this as a limitation: the cross-model behavioral effect is robust as a behavioral phenomenon but its construct interpretation as propensity remains unsettled by the M2 framing test."

These three branches are committed in `paper_experiments/m2_persona_decoupling/claim_surgery_M2_outcome_branches.md`. Day 5 selects the branch by pre-registered decision rule and inserts it verbatim into the rebuttal.
