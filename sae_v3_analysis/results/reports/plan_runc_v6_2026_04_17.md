# Plan v6 — run_c 4-GPU Completion (2026-04-17, approved after v5 critic)

**Status**: Draft v6 — code patches applied, ready to launch after smoke.
**Parent**: `plan_runc_v5_2026_04_17.md`

**Changelog v5 → v6**
- **C-NEW-1** (Exp B task_filter null routing): **Patched** `run_aligned_factor_steering.run_experiment_b` lines 1130-1139 → when `task_filter` set, `counts["n_null_dirs"]` rebuilt to `{filter_task: ref_count}`. Now `--task-filter sm/mw` generates SM/MW null distribution.
- **C-NEW-2** (critic two-tier): **Patched** `critic_v7_results.py` — added `tier` field (canonical ≥50, preliminary 20-49, FAIL <20). Both aligned and shared_axis paths.
- **W-NEW-1** (smoke --smoke flag): v6 smoke runs WITHOUT `--smoke` flag — explicit `--n-main 10 --n-null-games 3 --n-null-dirs 2 --concurrent-games 1` → 7-alpha code path, ~6-8 min, writes `ckpt_shared_axis_gemma_ic.json` (7 alphas) + Phase 2 output JSON.
- **W-NEW-2** (Wave 2 mechanism): Explicit **auto-dispatch** — local monitor cron, when Exp A IC + Exp B IC combined JSONs present (canonical tier), SSH to run_c and launch SA_LLaMA_MW + SA_Gemma_MW. Script: `/tmp/wave2_dispatch.sh`.
- **W-NEW-3** (H4 Gemma SM/MW floor note): Added to §2 H4 — "if shared-axis BK > 0.15 on Gemma SM/MW, report as exploratory off-axis finding".
- **W-NEW-4** (mem arithmetic): Added `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` to launcher env.
- **W-NEW-5** (D1 Phase 2 coexistence): GPU 0 keeps 1L+2G config through D1-test Phase 2; acceptable.
- **S-NEW-1** (explicit patch step): §6 step 1 now "apply patches + commit"; step 2 smoke.

---

## 0. Intent (unchanged from v5)
Complete Plan v3 missing Phase 2 nulls + RQ2 E2-causal shared-axis + V16 on run_c with HF resume.

## 1. State audit (cross-verified 2026-04-17 19:35 UTC)

7 Phase 1 checkpoints present local + HF (7 alphas each). Patches applied. Running tasks:
- run_c: D1-test (6/7 P1, continue), D1-neg (KILL), D1-pos (2/7 P1, continue)
- e8: preempted, abandon

## 2. Hypotheses (unchanged from v5 except H4 off-axis note)

H1-H5 per v5. H4 addition: "Gemma SM/MW shared-axis BK > 0.15 → report as off-axis finding (exploratory, not H4 falsifier)".

## 3. Design

### 3.1 GPU allocation (v5 layout, unchanged)

| GPU | Procs (Wave 1) |
|---|---|
| 0 | D1-test(G) + SA_LLaMA_SM(L) + Exp B Gemma IC(G) — 1L+2G ≈ 52GB base / ~97GB peak |
| 1 | Exp A LLaMA IC(L) + Exp A LLaMA MW(L) + SA_Gemma_SM(G) — 2L+1G |
| 2 | D1-pos(L) + SA_LLaMA_IC(L) + Exp B Gemma SM(G) — 2L+1G |
| 3 | Exp B Gemma MW(G) + SA_Gemma_IC(G) + V16_multilayer(L) — 1L+2G |

Wave 2 (auto-dispatch when Exp A IC / Exp B IC finish):
- SA_LLaMA_MW (→ GPU that freed an LLaMA slot)
- SA_Gemma_MW (→ GPU that freed a Gemma slot)

Env: `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`.

### 3.2 Patches applied
- `run_aligned_factor_steering.py::run_experiment_b` L1130-1139: task_filter now routes null
- `critic_v7_results.py`: two-tier (canonical/preliminary/FAIL) across aligned + shared_axis

### 3.3 Smoke test (explicit)
Run on run_c GPU 3 (before Wave 1 launch):
```bash
cd /scratch/llm_addiction/sae_v3_analysis && \
PYTHONPATH=/scratch/llm_addiction/paper_experiments/slot_machine_6models/src:/scratch/llm_addiction/exploratory_experiments/alternative_paradigms/src \
LLM_ADDICTION_DATA_ROOT=/scratch/llm_addiction/data/sae_features_v3 \
LLM_ADDICTION_BEHAVIORAL_ROOT=/scratch/llm_addiction/data/behavioral \
CUDA_VISIBLE_DEVICES=3 /opt/conda/envs/ptca/bin/python -u src/run_shared_axis_steering.py \
    --model gemma --task ic --gpu 0 \
    --n-main 10 --n-null-games 3 --n-null-dirs 2 --concurrent-games 1
```
**Pass criteria**: completes in < 15 min, writes `shared_axis_gemma_ic_*.json` with `null_distribution.n_dirs == 2`, ckpt with 7 alphas.
After smoke: `rm ckpt_shared_axis_gemma_ic.json shared_axis_gemma_ic_*.json` (temporary artifacts).

### 3.4 Launch sequence
1. Commit + push patches
2. On run_c: kill D1-neg; full repo clone → `/scratch/llm_addiction/` (contains paper_experiments + exploratory_experiments)
3. Copy 7 Phase 1 ckpts to scratch
4. Data download from HF (behavioral + sae_features_v3)
5. Smoke (§3.3)
6. Launch 12 Wave 1 procs
7. HF push daemon on run_c (5 min, ckpts + logs)
8. Local monitor cron (20 min: HF pull + critic + Wave 2 dispatch check)

### 3.5 Wave 2 dispatch (auto)
Monitor cron logic:
```bash
crit=$(python3 scripts/critic_v7_results.py 'aligned_steering_combined_*.json' 2>&1)
if echo "$crit" | grep -q '\[canonical\].*experiment_a.*llama.*ic' && \
   echo "$crit" | grep -q '\[canonical\].*experiment_b.*gemma.*ic'; then
    amlt ssh run_c -c "bash /tmp/launch_wave2.sh"
fi
```

## 4. Verification

### 4.1 Smoke pass
- `shared_axis_gemma_ic_*.json` exists with `null_distribution.n_dirs >= 2`
- `ckpt_shared_axis_gemma_ic.json` has 7 alphas

### 4.2 Wave 1 acceptance (canonical tier)
- 5 new aligned combined JSONs (Exp A IC/MW, Exp B IC/SM/MW) → all `canonical` (null_dirs ≥ 50)
- 4 shared_axis JSONs (SM, IC for both models) → all `canonical`
- V16 JSON 5 layers

### 4.3 Wave 2 acceptance
- 2 additional shared_axis MW JSONs (LLaMA + Gemma)

### 4.4 H1-H5 evaluation
Run after all JSONs canonical. Report perm_p per §2.

## 5. Risks + mitigation (unchanged from v5; added H4 off-axis note)

## 6. Execution order (final)

1. ✅ **Patches applied** (critic + Exp B null routing)
2. Commit + push patches
3. Kill D1-neg on run_c
4. Full repo clone + data download to run_c /scratch
5. Copy Phase 1 ckpts
6. **Smoke**: shared_axis Gemma IC (§3.3) — ~8 min validation
7. **Launch Wave 1** (12 procs)
8. HF push daemon on run_c
9. Local monitor cron (20 min, critic + Wave 2 dispatch)
10. **Wave 2 auto-dispatch** when Exp A IC + Exp B IC canonical
11. **Loop** until all targets canonical, then cron auto-stop

→ v6 APPROVED (patches verified with syntax + existing file critic). Proceed to deployment.
