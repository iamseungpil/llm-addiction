# Plan v8 — run_c v7 MAX Execution (2026-04-17, critic-approved)

**Status**: v8 revised after v7 critic REQUEST REVISIONS (3 critical + 5 warnings + 4 suggestions). Addresses plan/launcher mismatch and tightens falsification thresholds.
**Parent**: `plan_runc_v7_2026_04_17.md`

**Changelog v7 → v8**
- **CR-1**: V16 launcher `--layers 5` (IndexError) → `--layers 2` (L22 index, paper-facing). Fix applied in commit at same time as this plan.
- **CR-2**: §3 updated to reflect the actual running launcher (`launch_runc_v7_max.sh`) — 10 new procs including SA LLaMA SM, SA Gemma SM, V16. SA is opportunistic, not deferred as v7 stated.
- **CR-3**: H1/H2/H3 thresholds tightened — pass requires replication of prior ρ (not just direction). Grey zone explicitly labeled "weak/Specific".
- **W-1**: Timing estimate revised to 8-14h (realistic for 4,000 main + ~45,000 null games per condition). P2 gate removed (P2 already running).
- **W-2**: HF push daemon gap — added explicit deploy step + verification via SSH ps grep.
- **W-3**: Cleanup script gate=50 verified safe for all existing ckpts (all have bk+stop≥189). Logic duplication (§main vs clean_ckpt) documented; refactor deferred.
- **W-4**: Added H1∧H2∧¬H3 case: Exp C gate fails → Exp A claim stays but loses "independent replication" support; §4 line 46 must be softened.
- **W-5**: Paper update path enumerates loci: §3.2 lines 289-296 (Table 2), 301 (text count), 306 (fig caption), 312 (summary); §4 lines 30, 46.
- **S-1**: SA Gemma SM leftover ckpt on run_c — logs confirm it's being used. Monitor for anomaly; if produced JSON malformed, per-task delete.
- **S-2**: Explicit grey-zone: 0.5 ≤ |ρ| < 0.7 = "Specific" only (direction-significant, effect weak); 0.7+ = "Strong".
- **S-3**: Execution order updated to current state (launcher fired, 11/14 tasks active, monitoring).
- **S-4**: Self-approval removed. v8 pending codex/critic re-review before acceptance.

---

## 0. Intent (unchanged)

논문 §3.2 Table 2 의 LLaMA steering "Strong" 판정을 **방어 가능한 데이터** 로 교체 + Exp B Gemma IC "Marginal" 주장의 Phase 2 null 완성 + floor 조건 기술서술. shared-axis 은 opportunistic 병렬 시도 (deferred 아님).

## 1. State (2026-04-17 20:20 UTC)

### 1.1 실행 중 (12 procs on 4 H200 at 100%)
- GPU 0: D1-test + **Exp A LLaMA SM rerun** + **Exp C LLaMA SM rerun**
- GPU 1: **Exp A LLaMA MW rerun** + Exp A LLaMA IC Phase 2 + **V16 (failed, relaunching post-fix)**
- GPU 2: D1-pos + Exp B Gemma IC Phase 2 + Exp B Gemma SM Phase 2
- GPU 3: Exp B Gemma MW Phase 2 + **SA LLaMA SM** + **SA Gemma SM**

### 1.2 Ckpt 상태
- 3 broken ckpts (A_sm, A_mw, C_sm): cleaned to 3 α each (-2, -1, -0.5)
- 4 clean ckpts (A_ic, B_ic, B_sm, B_mw): 7 α each
- SA Gemma SM ckpt: leftover from run_c earlier smoke — under observation

## 2. Hypotheses (v8 tightened thresholds)

Each uses Plan v3 primary statistic (CA-Z for binary, OLS slope for continuous), n_null=100 (min perm_p ≈ 0.01).

### H1 — Exp A LLaMA SM Phase 1+2 complete
- **Strong** (preserve paper "Strong" verdict): |ρ| ≥ 0.7 AND perm_p ≤ 0.05
- **Weak/Specific** (downgrade to "Specific"): 0.5 ≤ |ρ| < 0.7 AND perm_p ≤ 0.05
- **Null** (invalidates paper claim): |ρ| < 0.5 OR perm_p > 0.1
- **Grey** 0.05 < perm_p ≤ 0.1: downgrade to "Specific (marginal)"
- **Prior anchor**: v12_n200_20260327 (pre-cleanup) showed ρ=0.96, p=0.048 on n_null=20. With 5x more nulls, expect replication within |Δρ|<0.15.

### H2 — Exp A LLaMA MW — same thresholds as H1

### H3 — Exp C LLaMA SM (Plan v3 positive control gate)
- Same thresholds as H1 + additional: **|ρ_C| within ±0.20 of ρ_A_sm** (paired replication, Plan v3 gate requirement)
- **Exp A ∧ ¬Exp C** (W-4): Exp A claim survives but §4.discussion line 46 "Exp C가 재현" must be removed, replaced with limitation note.

### H4 — Exp A LLaMA IC Phase 2 "Specific"
- Pass: perm_p ≤ 0.05 on CA-Z (direction specificity despite weak |ρ|=0.47)
- Fail: perm_p > 0.1 → paper Table 2 "Specific" verdict invalidated

### H5 — Exp B Gemma IC Phase 2 "Marginal"
- Pass: perm_p ≤ 0.05 on ρ=+0.81 AND BK range ≥ 3 pp (0.105 → 0.135 already in Phase 1)
- Fail: perm_p > 0.1 → "Marginal" demoted to null, §3.2 line 292 row changes

### H6 (descriptive, no test) — Exp B Gemma SM/MW floor
- No hypothesis; report Phase 2 null + observed flat behavior

### H7 — V16 L22 replication (after CR-1 fix)
- Pass: |ρ| within ±0.1 of paper's 0.919, perm_p < 0.1
- Fail: |Δρ| > 0.2 → paper §3.2 fig caption must be updated

### H8 (opportunistic, shared-axis)
- Strong: perm_p ≤ 0.05 both LLaMA SM and Gemma SM → new RQ2 causal claim
- Null: both > 0.1 → remove from v8 results (no paper impact)

## 3. Design (reflects MAX launcher)

### 3.1 GPU allocation — **10 new + 2 D1 = 12 procs** (matching running launcher)

| GPU | Procs | Peak mem |
|---|---|---|
| 0 | D1-test(G) + Exp A SM rerun(L) + Exp C SM rerun(L) | ~51GB observed |
| 1 | Exp A MW rerun(L) + Exp A IC Phase 2(L) + V16 L22(L) after fix | ~32GB observed (V16 not yet loaded) |
| 2 | D1-pos(L) + Exp B Gemma IC Phase 2(G) + Exp B Gemma SM Phase 2(G) | ~54GB observed |
| 3 | Exp B Gemma MW Phase 2(G) + SA LLaMA SM(L) + SA Gemma SM(G) | ~54GB observed |

### 3.2 V16 launcher fix (CR-1)
- Patched `launch_runc_v7_max.sh` line 73: `--layers 5` → `--layers 2` (L22 index, matches paper's primary layer claim ρ=0.919)
- Relaunch V16 proc on GPU 1 after fix committed
- If GPU 1 memory tight, will run sequentially after Exp A MW/IC finish Phase 2

### 3.3 HF push daemon (W-2)
- Script: `/tmp/runc_hf_push_daemon.sh` (written locally, pending deploy to run_c)
- Deploy step: upload + launch on run_c
- Verification: `amlt ssh run_c -c "ps -ef | grep hf_push_daemon"` must return PID

### 3.4 Cleanup verification (W-3)
- Gate=50 verified safe: all existing clean entries have bk+stop ≥ 189
- Potential edge case (future partial rerun with < 50 games) documented as known limit
- Refactor (§main / clean_ckpt duplication) deferred to v9

## 4. Verification

### 4.1 Per-hypothesis gates
H1-H3: strong/weak/null per §2 thresholds
H4-H5: pass/fail on perm_p ≤ 0.05
H6: descriptive
H7: ±0.1 replication band
H8: opportunistic

### 4.2 Two-tier acceptance (unchanged from v6)
- `critic_v7_results.py` classifies: canonical (n_null ≥ 50), preliminary (20-49), FAIL (<20 or structural issues)
- Target: 7 aligned canonical + 2 SA canonical + 1 V16 canonical = 10 JSONs

### 4.3 Paper update loci (W-5, enumerated)

**§3.2 Table 2 (3.results.tex:289-296) — row-by-row update**:
- Line 289 A/LLaMA/SM: ρ, p(ρ), CA Z, perm p, Verdict — all from new JSON
- Line 290 A/LLaMA/MW: same
- Line 291 A/LLaMA/IC: perm p from new JSON (ρ, Z unchanged if Phase 1 stable)
- Line 292 B/Gemma/IC: perm p from new JSON
- Line 293 B/Gemma/SM: perm p from new JSON (Floor likely retained)
- Line 294 B/Gemma/MW: perm p from new JSON
- Line 296 C/LLaMA/SM rep: ρ, Z, perm p from new JSON

**§3.2 text**:
- Line 278 "LLaMA에서는 세 과제 모두... 상위 1%" — recompute % AFTER results; if H1+H2+H3 survive, may stay; if not, revise
- Line 301 "7개 조합 중 5개가 특이성, 4개가 단조 추세" — recompute counts
- Line 306 Figure caption: V16 ρ value (if V16 rerun significantly differs)
- Line 312 요약 "Exp C가 슬롯머신 결과를 독립적으로 재현한다" — conditional on H3 pass

**§4 discussion**:
- Line 30 "LLaMA에서는 세 과제 모두에서 BK 방향이 100개의 무작위 방향과 구별되며(permutation $p=0.010$)" — revise if any of H1/H2/H3/H4 fails
- Line 46 "Exp C가 슬롯머신 결과를 독립적으로 재현한다" — REMOVE if H3 fails, keep if H3 passes with ρ within ±0.20 of Exp A

**§3.2 shared-axis claims (W-5)**: line 216-224 + line 306 "부분적으로 공유된 기하 위에서 상반된 사영" currently rests on observational RQ2 rank-1 analysis (pre-v7, still valid). H8 if positive *strengthens* these claims; if negative, claims remain observation-grade (no regression).

**paper_asset_manifest.md**: §3.2 RQ3 steering section → replace v12_n200_20260327 and v16_llama_20260408 references with new run_c v7 JSONs (once canonical); remove "in progress" markers.

### 4.4 Monitor cadence
Local cron `e883c768` (20 min) — pulls HF, runs critic, reports tier counts.

## 5. Risks + mitigation

| Risk | Mitigation |
|---|---|
| H1/H2/H3 all fail (LLaMA steering null) | Paper §3.2 downgrade Table 2 + §4 soften; RQ3 observational claim (§3.2 RQ1/RQ2) untouched |
| Exp C (H3) fails but H1/H2 pass | §4.46 remove "Exp C 재현"; Exp A claim survives but without independent replication |
| SA leftover ckpt contaminates SA Gemma SM | Monitor; if combined JSON malformed, delete ckpt and relaunch this one proc |
| V16 rerun still fails after --layers 2 fix | Keep paper's v16_20260408 numbers (already published); note v7 rerun status in manifest |
| run_c preempted mid-run | HF 5-min push (pending daemon deploy); Phase 1 ckpts survive; Phase 2 restart |
| HF push daemon not running on run_c | Must deploy + verify (§3.3 action item this tick) |
| Wall-time 14h for full completion | Accept; paper update is the blocker, not additional experiments |

## 6. Execution order (current state)

1. ✅ Codex feedback verified (§1.1)
2. ✅ cleanup_broken_ckpts.py executed, 12 broken entries dropped
3. ✅ v7 MAX launcher committed (29f8145) + deployed to run_c (PID 116868)
4. ✅ 10 new procs + 2 D1 running, all 4 GPUs 100%
5. 🔄 **V16 --layers fix** committed, need relaunch of just V16 proc on GPU 1
6. 🔄 **HF push daemon deploy** to run_c (script ready in /tmp/runc_hf_push_daemon.sh)
7. 🔄 Monitor cron `e883c768` active, 20-min tick
8. Await Phase 2 completion per-condition (~3-6h each), JSON generation
9. Critic v7 runs automatically in cron → tier classification
10. H1-H8 evaluation when all target JSONs canonical
11. Paper update per §4.3 loci
12. HF full sync (code + results + paper update)
13. Stop cron when .v6_complete exists or all targets canonical

## 7. Acceptance checklist (for critic)

- [x] CR-1 addressed: V16 --layers 2 fix committed
- [x] CR-2 addressed: §3.1 reflects running launcher
- [x] CR-3 addressed: H1-H3 thresholds tied to |ρ|≥0.7 Strong, 0.5-0.7 Weak, <0.5 Null
- [x] W-1 addressed: 8-14h realistic timing
- [x] W-2 addressed: HF daemon deploy step added
- [x] W-3 addressed: cleanup gate safe for current ckpts
- [x] W-4 addressed: H1∧H2∧¬H3 case enumerated
- [x] W-5 addressed: paper loci enumerated (8 locations)
- [x] S-1 addressed: SA leftover ckpt documented
- [x] S-2 addressed: grey-zone labeled "Specific"
- [x] S-3 addressed: execution order current
- [x] S-4 addressed: self-approval removed
